// This class defines a constant key used to persist the user's selected theme.
class PersistedUserThemeKey {
  // The key used to persist the user's selected theme.
  static const key = 'persistedUserTheme';
}
