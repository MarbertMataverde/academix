import 'package:flutter/material.dart';

void main() {
  runApp(const Academix());
}

class Academix extends StatelessWidget {
  const Academix({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
