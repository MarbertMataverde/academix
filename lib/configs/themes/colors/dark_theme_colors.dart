import 'package:flutter/material.dart';

// This class defines a set of color constants for the dark theme in the application.
class DarkThemeColors {
  // The primary color used in the light theme.
  static const Color primaryColor = Color(0xFFBFBDB6);

  // The secondary color used in the light theme.
  static const Color secondaryColor = Color(0xFF565B66);

  // The background color for the scaffold (the main app screen) in the dark theme.
  static const Color scaffoldBackgroundColor = Color(0xFF0B0E14);

  // The foreground color used for body text in the dark theme.
  static const Color foregroundColor = Color(0xFFBFBDB6);

  // The secondary foreground color used for labels in the dark theme.
  static const Color foregroundSecondColor = Color(0xFF565B66);

  // The error color used to indicate errors in the dark theme.
  static const Color errorColor = Color(0xFFC95152);

  // The shadow color used in the dark theme for visual depth.
  static const Color shadowColor = Color(0xFF05070A);

  // Dark button background color
  static const Color buttonBackgroundColor = Color(0xFF1B1F25);

  // Dark navigation bar button background color
  static const Color navigationButtonColor = Color(0xFF1B1F25);
}
