import 'package:flutter/material.dart';

// This class defines a set of color constants for the light theme in the application.
class LightThemeColors {
  // The primary color used in the light theme.
  static const Color primaryColor = Color(0xFF5C6166);

  // The secondary color used in the light theme.
  static const Color secondaryColor = Color(0xFF8A9199);

  // The background color for the scaffold (the main app screen) in the light theme.
  static const Color scaffoldBackgroundColor = Color(0xFFF8F9FA);

  // The foreground color used for body text in the light theme.
  static const Color foregroundColor = Color(0xFF5C6166);

  // The secondary foreground color used for labels in the light theme.
  static const Color foregroundSecondColor = Color(0xFF8A9199);

  // The error color used to indicate errors in the light theme.
  static const Color errorColor = Color(0xFFE65050);

  // The shadow color used in the light theme for visual depth.
  static const Color shadowColor = Color(0xFFD3D4D5);

  //  Light button background color
  static const Color buttonBackgroundColor = Color(0xFFFCFCFC);

  // Light navigation bar button background color
  static const Color navigationButtonColor = Color(0xFFD3D4D5);
}
