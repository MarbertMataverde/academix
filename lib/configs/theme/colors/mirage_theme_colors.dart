import 'package:flutter/material.dart';

// This class defines a set of color constants for the Mirage theme in the application.
class MirageThemeColors {
  // The primary color used in the Mirage theme.
  static const Color primaryColor = Color(0xFFFFCC66);

  // The background color for the scaffold (the main app screen) in the Mirage theme.
  static const Color scaffoldBackgroundColor = Color(0xFF242936);

  // The foreground color used for body text in the Mirage theme.
  static const Color foregroundColor = Color(0xFFCCCAC2);

  // The secondary foreground color used for labels in the Mirage theme.
  static const Color foregroundSecondColor = Color(0xFF707A8C);

  // The error color used to indicate errors in the Mirage theme.
  static const Color errorColor = Color(0xFFFF6666);

  // The shadow color used in the Mirage theme for visual depth.
  static const Color shadowColor = Color(0xFF161A22);
}
